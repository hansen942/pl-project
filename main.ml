open Definitions
open Typecheck
open Evallambda

(* TODO: To achieve full functionality we need to allow type definitions. This will require that we change the [typecheck] function to accept a type environment data structure that allows us to keep track of user-defined types *)
let eval_prog prog = failwith "unimplemented"
